# Roland Wayne 网站重构设计方向

状态：视觉方向已确定，首页、文章归档和文章详情已完成首轮实现。

旧站的深色 `#0A0A0A` 与金色 `#C9A96E` 只作为历史回归资料，不再约束新站。本轮主题以 Wayne InsightSpring 的 WIS 品牌为视觉锚点，采用深蓝、冰蓝、冷白和近黑色。

## 1. 核心方向

- 桌面端使用固定深蓝窄侧栏，承载人物身份、主导航和语言入口。
- 主内容是一张连续的冷白画布，不把 Hero 文案与 WIS 图形切成两个独立面板。
- 首页保留清晰的编辑结构：品牌 Hero、最新文章时间线、工作与实践、联系入口、标准页脚。
- 视觉上的“Apple 风格”指克制、秩序、留白和精细排版，不复制 Apple 的页面资产。
- 所有内容继续由当前 React/Vite 项目承载，不依赖外部网页模板。

参考来源：

- [Julian Schrittwieser](https://www.julian.ac/)：左侧人物身份栏、圆形头像和安静的个人索引。
- [Andrej Karpathy](https://karpathy.ai/)：时间线阅读节奏与文章内容组织。
- 项目内 WIS Logo、真实人物照片、博客文章和研究/服务/高校合作内容。

## 2. 固定身份栏

### 桌面端

- 宽度固定为紧凑窄栏，整块使用 WIS 深蓝色。
- 顶部使用真实圆形头像，下面只显示 `Roland Wayne`，不再附加身份 title。
- 主导航为：关于、文章、研究、服务、高校合作、联系。
- 当前页面只用一个冰蓝小圆点标识，不使用斜杠、胶囊底或粗边框。
- 站点采用单一中文界面，侧栏底部保持纯深蓝留白，不显示语言切换或双页面入口。

### 移动端

- 固定侧栏折叠为深蓝顶部身份条。
- 头像、姓名和菜单按钮保持同一视觉层级。
- 导航进入全屏菜单，支持键盘、`aria-expanded` 和关闭后的滚动恢复。

## 3. 主内容画布

### Hero

- 整个 Hero 使用同一个冷白底色 `--wis-paper`。
- 文案与 WIS Logo 叠加在同一连续画布上，不设置中间分割线或独立右侧背景。
- 透明几何斜面覆盖全宽并由左向右逐渐增强；左侧保持足够淡，确保标题可读。底图始终完整映射原始画面，不使用会放大裁切取景的 `cover`。
- WIS Logo 使用项目提供的真实 Logo 资源，不重新绘制、不做渐变字标；Logo 视觉中心与左侧整组文案的中心线严格对齐。
- Hero 标题第一行使用 WIS 深蓝，第二行“拆解复杂问题”使用近黑色，形成明确但克制的双色层级。

### 最新文章

- 使用 Karpathy 式细线时间轴，但控制为最多五篇。
- `Welcome — Why This Site Exists` 通过 frontmatter 的 `pinned: true` 固定置顶。
- 其余文章按发布日期倒序。新增 Markdown 文章会自动出现在首页，无需手写首页列表。
- 每篇显示封面、日期、标题和摘要；超过五篇通过“查看全部文章”进入 `/blog`。

### 工作与实践

- 首页保留学术研究、独立写作、系统构建三项内容。
- 使用与其他正文区一致的 `--wis-paper` 冷白底，并以线框、留白和轻量图标组织，不做浮起卡片或重阴影。

### 联系与页脚

- “想聊聊？”区继续使用与 Hero/最新文章一致的 `--wis-paper` 冷白底，作为正文画布的自然收束。
- 标准 SEO 页脚单独使用近黑色 `#05070D`，与白色联系区形成明确边界。
- 页脚位于主内容列内：桌面端从固定侧栏右缘开始，移动端恢复全宽。
- 页脚包含站内导航、个人主页、友情链接、版权和所在地。

## 4. 视觉 Token

```text
--wis-navy: 深蓝品牌主色
--wis-blue: 交互与小标题强调色
--wis-ice: 冰蓝辅助色
--wis-paper: 全站统一冷白内容底
--wis-surface: 浅蓝灰节奏分区
--wis-copy: 正文灰蓝色
--wis-line: 1px 冷色 hairline
--sidebar-width: 桌面窄侧栏宽度
```

原则：系统字体全站统一；标题依靠字号、字重和留白形成高级感，不依靠衬线体、金色、拟物雕塑或强渐变。

## 5. 首页信息架构

```text
固定深蓝身份栏 │ 连续冷白 Hero：标题 + 透明几何面 + WIS Logo
               │ 最新文章：最多五篇的自动时间线
               │ 工作与实践：研究 / 写作 / 系统构建
               │ 冷白联系区：想聊聊？
               │ 近黑标准页脚：导航 / 个人主页 / 友情链接
```

## 6. 功能保留合同

重构必须继续支持：

- `/` 作为规范关于/首页，`/about` 保留重定向。
- 博客 Markdown collection、draft 过滤、置顶、日期排序、动态 slug 和最多五篇首页自动更新。
- 文章正文中的现有配图、引用、标签和发布日期。
- 研究、服务、高校合作、关于和联系内容。
- 移动端导航和所有站内 CTA。
- canonical、Open Graph、Twitter Card、favicon、robots、sitemap、Person 与 WebSite JSON-LD。
- Cloudflare adapter 和 `wrangler.jsonc` 构建配置。

## 7. 验收标准

- Hero 是一张连续画布，不出现左右面板割裂感。
- 所有白色正文区使用相同的 `--wis-paper`；只有节奏分区使用明确的浅蓝灰，页脚使用近黑色。
- 左侧人物栏在桌面端稳定，移动端正确折叠。
- 首页文章从内容集合自动生成，Welcome 始终置顶，最多显示五篇。
- 所有现有路由、内容、资源和 SEO 要素通过 `DEVELOPMENT.md` 的回归清单。
- 桌面、移动、菜单、文章归档和文章详情完成浏览器视觉回归，生产构建通过。
